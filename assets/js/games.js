// games.js — Game CRUD Operations
// - addGame(gameData): creates new game doc in Firestore
// - getGames(uid): fetches all games for a user
// - getGameById(uid, gameId): fetches single game doc
// - updateGame(uid, gameId, updatedData): updates game fields
// - deleteGame(uid, gameId): deletes game doc
// - toggleFavorite(uid, gameId): flips the favorite boolean
// - updatePlayStatus(uid, gameId, status): sets played/playing/plan-to-play
// - Exports: addGame, getGames, getGameById, updateGame, deleteGame, toggleFavorite, updatePlayStatus
