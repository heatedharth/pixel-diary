# Firebase config (contains sensitive API keys)
firebase.config.js

# OS files
.DS_Store
Thumbs.db

# Uploaded media (large files, stored in Firebase Storage)
assets/images/uploads/

# Node (if any build tools are added later)
node_modules/
.env
