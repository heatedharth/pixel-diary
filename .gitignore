# Firebase config (no longer used — kept here in case file still exists locally)
firebase.config.js

# OS files
.DS_Store
Thumbs.db

# Uploaded media (stored in Supabase Storage)
assets/images/uploads/

# Node (if any build tools are added later)
node_modules/
.env

# RAWG and YouTube API
api.config.js